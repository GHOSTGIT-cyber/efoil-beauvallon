# === Medias : versionnes (incl. medias/web/ + reels compresses) ===
# Les versions web optimisees (WebP + mp4) sont suivies par git.
# Seuls les originaux video lourds restent hors git (voir assets/video/ plus bas).

# === OS / IDE ===
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/settings.json
*.swp
*.swo

# === Node (si on ajoute un build tool plus tard) ===
node_modules/
.env
.env.local

# === WordPress local ===
wordpress/*.log

# === Videos lourdes : hors git (hebergement externe / Git LFS au deploiement) ===
assets/video/

# === Squad d'agents SEO/GEO (copie locale, hors git) ===
11+Agents+SEO+_+GEO+-+RosoAI/
